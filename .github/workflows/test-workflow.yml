name: Test-Workflow

on:
  schedule:
    - cron: "0 16 * * *"  # 8:30 pm IST (3:00 pm [15:00] UTC)

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    permissions: read-all
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Run code
        run: |
          echo "Hello Workflow!"
          whoami
          pwd
